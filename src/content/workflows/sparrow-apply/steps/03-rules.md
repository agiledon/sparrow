# 执行规则

读 `references/apply-rules.md`：按任务执行方执行；领域层 TDD 同一步骤内完成；遵守 harness `apply/implementation.md`。

revise 且档位 S4：按该文件中的代码动作映射执行，校验边界后再更新 code_review。

- 若为交互上下文：下一步只读取 `steps/04-interaction.md`。
- 否则不要打开交互实现说明。下一步只读取 `steps/05-review.md`。
