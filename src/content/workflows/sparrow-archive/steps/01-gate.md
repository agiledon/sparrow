# 确认状态并读取契约

读 `references/archive-gate.md`（完成判定、完整/部分归档、promote 命令）。语义以该契约为准。

`scripts/sparrow-state.mjs show`。`tbd` → **sparrow-requirement**；`brownfield` → 停止。通过后 `set-step archive ongoing`。

下一步：读取 `steps/02-select.md`。不要提前打开它。
