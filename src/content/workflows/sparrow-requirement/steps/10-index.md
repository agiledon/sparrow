# 更新项目向导

读 `references/project-md-update.md` 与 `references/version-metadata.md`，更新已存在的 change 工作区 `project.md`（§1.1 只勾选 catalog；不在此步才首次创建）。

运行 `scripts/sparrow-state.mjs set-step requirement done`。

本阶段结束。不要再打开未使用的模板。
