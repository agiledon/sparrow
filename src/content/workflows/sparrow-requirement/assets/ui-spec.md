# {系统名称} UI 规格

## {角色1名称}

### 用户画像

- 角色：{角色名称}
- 核心目标：{目标}
- 技术熟练度：{熟练度}
- 使用场景：{场景}

### 端到端操作流程

> **输入** = `requirement/business/catalog.md` 中已确认的端到端业务流程。
> 禁止另起与端到端业务流程脱节的旅程。每条操作流程必须可回溯到 `EBP-*`。

#### 操作流程：{名称}（← EBP-{id}）

| 序 | 用户操作 | 系统反馈 | 对应业务服务 | 页面触点 |
|----|----------|----------|---------|----------|
| 1 | {操作} | {反馈} | BS-… | {页面名} |
| 2 | {操作} | {反馈} | BS-… | {页面名} |

覆盖自检：流程步 ↔ 页面无断点、无孤儿页。

### UI 页面

#### {页面1名称}

- **页面路径**：/{path}
- **页面目的**：{解决的操作流程触点；回溯 EBP-{id} 步骤 n}
- **页面结构**：

```
{ASCII 主要布局区域}
```

- **交互方式**：

| 操作 | 触发元素 | 系统响应 |
|------|---------|---------|
| {操作1} | {触发} | {响应} |

## {角色2名称}

（按同一结构继续）
