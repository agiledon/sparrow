# 分类

分类：业务需求 → `requirement/business/`（catalog / business-services 等）；质量属性 → `requirement/quality/quality.md`；技术/NFR → api/tech；领域模型 → model.md；UI → `requirement/ui/**`；实现纪律 → harness。

下一步：读取 `steps/04-constraint.md`。不要提前打开它。
