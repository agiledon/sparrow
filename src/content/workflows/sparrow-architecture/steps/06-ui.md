# 可选前端架构

读 `references/frontend-architecture.md`。按 `assets/frontend.md` 与 `assets/spec-interaction.md` 写入 `architecture/frontend.md` 与 `design/{ui-slug}/spec.md`（业务服务源改为 catalog 所链 `business-services.md#BS-*`）。

若 `requirement/ui/` 不存在：不要写上述文件；按 `references/frontend-architecture.md` 询问是否补 UI。用户拒绝则不要打开那两份模板。

- 若本次为 revise：下一步只读取 `steps/07-revise.md`。
- 否则下一步只读取 `steps/08-index.md`。不要打开未选中的文件。
