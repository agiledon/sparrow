# 映射限界上下文

读取 catalog 与各 `{sd-slug}/subdomain.md`。读 `references/react-mapping.md`。先将每个 SD **一对一**映射为 BC，再按 ReAct 识别拆分/合并等调整；遵守 harness `arch/bounded-contexts.md`。

按 `assets/bounded-contexts.md` 写入 `architecture/bounded-contexts.md`。

下一步：读取 `steps/05-slice.md`。不要提前打开它。
