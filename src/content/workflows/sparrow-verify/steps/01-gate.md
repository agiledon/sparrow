# 确认状态与 slug

运行本 skill 的 `scripts/sparrow-state.mjs show`。`tbd` 则先 **sparrow-requirement**；`brownfield` 则停止。未指定 slug 时从 `project.md` 列出，可选单个、多个或全部。对每个 slug：`set-context {slug} verify ongoing`。

下一步：读取 `steps/02-language.md`。不要提前打开它。
