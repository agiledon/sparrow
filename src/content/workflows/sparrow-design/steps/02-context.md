# 读取上下文并分支

读 `references/ubiquitous-language.md`、`references/revise-gate.md`、`references/output-existence-check.md`。未指定 slug 时从 `project.md` 列出并请用户选择。

- 标注 `— *交互上下文*`：下一步只读取 `steps/03-interaction.md`。
- 否则：下一步只读取 `steps/03-backend.md`。

不要读取未选中的文件。
