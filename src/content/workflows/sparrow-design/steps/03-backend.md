# 后端契约

读 `references/api-design-rules.md`。按 `assets/tech.md` 与 `assets/api.md` 写入 `design/{slug}/tech.md` 与 `design/{slug}/api.md`。

每次一问选择语言与技术栈。一个业务服务 = 一个序列图 = 一个 API；序列图只含 BC 与外部系统。

不要打开交互上下文模板。

下一步：读取 `steps/04-catalog.md`。不要提前打开它。
