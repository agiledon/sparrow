# 交互契约

读 `references/interaction-design.md`。按 `assets/tech-interaction.md` 与 `assets/api-interaction.md` 写入 `design/{slug}/tech.md` 与 `design/{slug}/api.md`。

不读取任何 BC 的 `api.md`。BFF 只聚合不写业务逻辑。

不要打开后端 BC 的 api/tech 模板。

下一步：读取 `steps/04-catalog.md`。不要提前打开它。
