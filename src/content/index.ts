/**
 * Content layer — schema authority and bundled workflow/harness assets.
 * Workflow markdown lives under src/schemas until fully migrated to content/workflows/.
 */
export { getWorkflowSchema } from '../kernel/content/schema.js';
