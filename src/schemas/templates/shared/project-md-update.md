# project.md 更新

路径：`docs/sparrow/change/current/{activeChangeId}/project.md`。

1. 若不存在，按 `assets/project.md`（或本 skill 拷贝的共享模板）创建。
2. 将本阶段已生成条目从 `*待生成*` 改为已完成（change 工作区不写 version 号；master 副本用 `*v{version}*`）。
3. 更新文件头部「最后更新」时间戳。
4. 基线（无活动变更、首次需求）**不写**「变更管理」块、不标注档位。revise 模式下才创建/更新变更管理块与 BC 进度档位。
5. 不改动未本阶段负责的 BC 列表结构，除非本阶段职责就是新增/调整 slug 索引（sparrow-arch）。
