## 执行顺序检查

- **本阶段**：sparrow-arch（第 2 步 / 共 8 步），产品级
- **前置**：`change/current/{activeChangeId}/requirement/business/prd-business.md` 必须存在，否则先 **sparrow-requirement**
- 已存在 `architecture/business.md` 或 `application.md` 时按存在性检查处理
