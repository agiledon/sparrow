## 执行顺序检查

- **本阶段**：sparrow-design（第 3 步 / 共 8 步），团队级，按 slug
- **前置**：`sparrow-state.json` 的 development-mode 非 `tbd`/`brownfield`；`architecture/bounded-contexts.md`（后端）或 `architecture/frontend.md`（交互上下文），以及 `design/{slug}/spec.md`
- 未指定 slug 时从 `project.md` 列出。按是否标注「交互上下文」分支
