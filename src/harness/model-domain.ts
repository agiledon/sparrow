/**
 * Model stage domain-modeling constraints — static/dynamic modeling and OOP discipline.
 */

export const MODEL_DOMAIN_BODY = `# 领域建模约束（model / domain-modeling）

本文件定义 sparrow-model 阶段**静态 / 动态建模与面向对象设计**的纪律。

## 三阶段建模流程

1. **阶段一（静态建模）**：统一语言 → 实体 / 值对象识别 → 关系建模 → 聚合识别 → 聚合根操作定义 → 类图绘制。
2. **阶段二（动态建模）**：API 入口提取 → 任务分解 → 角色构造型分配 → 序列图绘制。
3. **阶段三（动静结合）**：提取序列图职责 → 映射到静态模型 → 更新类图 → 一致性验证。

## 优先级

1. 当阶段一与阶段三存在差异时，**始终以阶段三从序列图提取到的操作为准**。
2. 术语统一使用"**操作**"（operation），对应 UML 的 method。

## 聚合纪律

1. **聚合 ≤ 事务边界**：一个聚合的边界即一次事务的边界；聚合内强一致，聚合间最终一致。
2. **聚合间通过聚合根 ID 引用**，不直接持有对象引用（迪米特法则）。
3. **一个聚合对应一个 Repository**。
4. 值对象必须不可变（构造时初始化所有字段，无修改方法）。

## 面向对象设计原则

### 信息专家（Information Expert）
1. 将操作分配给**拥有该操作所需数据的对象**。
2. 自问：哪个对象拥有完成这个操作所需的数据？答案就是该操作的归属。
3. 反例：计算订单总额的操作应分配给 \`Order\` 聚合根，而非外部服务。

### 迪米特法则（Law of Demeter）
1. 每个对象只与它的直接朋友通信，避免"链式调用"。
2. 聚合内部实体之间的交互应通过聚合根协调，不应跨过聚合根直接访问内部实体。
3. 聚合之间通过聚合根 ID 引用，而非直接持有对象引用。
4. 反模式：\`order.getCustomer().getAddress().getCity()\`（穿越多个对象边界）。
5. 正例：\`order.getCustomerCity()\`。

### 避免贫血模型（Anti-Anemic Domain Model）
1. 领域对象应包含业务行为，而非仅作为数据容器。
2. **禁止为每个属性都定义 \`getXxx()\` / \`setXxx()\` 操作**。
3. 只定义代表真实业务含义的操作（如 \`approve()\`、\`cancel()\`、\`addLineItem()\`）。
4. 属性暴露以**最小必要**为原则。

### 封装原则（Encapsulation）
1. 对象的内部状态（属性）不应随意对外暴露。
2. 状态变更必须通过业务操作进行，确保不变量始终得到保护。
3. 值对象必须不可变。
4. 聚合根对外提供行为方法，隐藏内部实体结构和状态细节。
5. 类图属性统一使用 \`-\`（私有），操作使用 \`+\`（公开）。

## 聚合根合法业务操作类型

- **构造 / 工厂操作**：创建聚合实例（\`placeOrder()\`、\`scheduleMeeting()\`）
- **状态变更操作**：体现业务意图的状态转换（\`start()\`、\`cancel()\`、\`approve()\`、\`reject()\`）
- **实体管理操作**：管理聚合内子实体（\`addParticipant()\`、\`removeLineItem()\`）
- **计算 / 查询操作**：基于聚合内部状态的计算（\`calculateTotal()\`、\`isOverdue()\`）
- **业务规则校验**：聚合级别的业务规则检查（\`canBeModified()\`、\`isValidForSubmission()\`）

## 禁止的操作模式

- ❌ 裸露的 getter / setter：\`getName()\`、\`setName()\` → 破坏封装，导致贫血模型
- ❌ 纯数据存取方法：为每个属性生成成对的 get / set → 等同于把聚合当数据容器
- ❌ 跨聚合访问方法：\`getOtherAggregate()\` → 违反迪米特法则
- ❌ 无业务含义的 CRUD：\`update()\`、\`save()\`、\`delete()\` → 由领域服务和端口承担`;
