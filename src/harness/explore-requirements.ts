/**
 * Explore stage constraints — business service identification discipline.
 */

export const EXPLORE_REQUIREMENTS_BODY = `# 需求探索约束（explore / requirements）

本文件定义 sparrow-explore 阶段**必须遵守 / 禁止**的业务服务识别纪律。

## 必须（MUST）

1. **必须全面覆盖原始需求**：原始需求中的每个功能点、每个业务场景、每个用户需求都必须被识别为对应的业务服务，不能有任何遗漏。
2. **业务服务名称必须为动词短语（动宾结构）**：如"提交订单"、"用户注册"、"申请贷款"。禁止使用技术术语作为服务名。
3. **必须包含完整组成元素**：每个业务服务必须包含服务编号、服务名、服务描述、触发事件、基本流程、替代流程、验收标准。
4. **服务描述格式**："作为<角色>，我想要<服务功能>，以便<服务价值>"。
5. **必须区分参与者类型**：用户（user）、策略（policy）、伴生系统（accompanying system）。
6. **必须覆盖异常路径**：替代流程覆盖主要的失败场景；验收标准可测试、可验证。

## 禁止（MUST NOT）

1. **禁止将"用例"或"模块"识别为业务服务**：业务服务 ≠ 用例。管理类模块（如"用户管理"、"个人资料管理"）包含多个独立请求，必须拆分为独立的业务服务。
2. **禁止将多次独立请求合并为一个业务服务**：一次请求 = 一个业务服务。
3. **禁止将一次操作拆分为多个业务服务**。
4. **禁止包含线下执行步骤**：执行步骤仅限于系统后端收到请求后执行的一次连续而完整的功能交互，不包括前端操作和线下动作。
5. **禁止引入纯技术功能**：没有实际业务价值的纯技术功能不应被识别为业务服务。

## 判断标准（一次请求原则）

当且仅当以下三个问题的答案都为"是"，才识别为一个独立的业务服务：

- **请求次数**：参与者向目标系统发起了独立的一次请求？
- **独立价值**：该请求完成后产生了明确的、对参与者有价值的业务结果？
- **独立触发**：该请求可以独立触发，而不依赖其他请求先执行？

## Grill Me 提问纪律（阶段一）

1. **每次只问一个问题**，给出推荐答案，并在问题末尾明确提示确认选项：是 / 否 / 修改。
2. 至少覆盖以下维度：参与者识别、核心流程、业务规则、边界条件、异常场景、质量属性。
3. 按维度有序推进：先核心流程与参与者，再业务规则与边界条件，最后异常场景与质量属性。
4. **不为了提问而提问**：某一维度已经清晰时不再追问。

## 质量属性文档纪律（阶段二）

1. **只覆盖需求探索中实际涉及的维度**，未涉及的维度直接省略，不提供空章节或"暂不考虑"等占位文字。`;
