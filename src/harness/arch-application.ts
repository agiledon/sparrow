/**
 * Application architecture constraints — bounded context and communication discipline.
 */

export const ARCH_APPLICATION_BODY = `# 应用架构约束（arch / application）

本文件定义 sparrow-arch 阶段二（应用架构映射）**必须遵守 / 禁止**的限界上下文与通信纪律。

## 命名规范

1. **限界上下文名称必须为名词**，精简抽象，体现核心能力。
2. **禁止含"系统""服务"后缀**。
   - ✅ 正确示例：\`库存\`、\`支付\`、\`用户\`、\`订单\`、\`计费\`、\`通知\`
   - ❌ 错误示例：\`订单管理系统\`（含"系统"后缀）、\`用户服务\`（含"服务"后缀）、\`数据统计分析\`（含动词）

## 上下游关系

1. 限界上下文之间如果是客户-供应方的上下游关系，**禁止出现循环依赖**（即 A 既是 B 的上游、B 也是 A 的上游）。

## 自治原则

1. 每个限界上下文拥有的领域知识都应是**最小完备且能自我履行**的。
2. 限界上下文的逻辑边界应**涵盖领域层对应的数据模型**。
3. 限界上下文的隔离性仅体现为**逻辑隔离**；**限界上下文并不必然设计为微服务**。

## 通信方式

1. **同一进程**：参与通信的 BC 通过**下游 BC 的南向网关 Client** 发起对**上游 BC 的北向网关本地服务**的调用。
2. **不同进程**：通过**公开 API** 或**领域事件**通信。
3. **无论是否同一进程，都禁止直接跨 BC 访问领域对象**。

## 团队对齐

1. 一个限界上下文的所有服务契约应归属于**同一个团队**，不允许一个 BC 由多个特性团队共同维护。

## 上下文映射

1. 所有跨 BC 关系必须明确标注上下文映射模式，并说明选择理由：
   - 防腐层（ACL）/ 开放主机服务（OHS）/ 遵奉者（Conformist）/ 客户-供应商（Customer-Supplier）/ 共享内核（Shared Kernel）/ **发布-订阅（Publisher-Subscriber）** / 分离方式（Separate Ways）
2. 发布-订阅模式表示采用**事件发布和订阅**的通信模式。

## 拆分纪律

1. **不应过度拆分**：若同一子领域内的两个服务"经常需要协作才能完成一次业务请求"，则不应拆分为两个 BC（内聚优先）。
2. **奥卡姆剃刀原则**：合并在前，拆分在后；默认不拆分。拆分的充分依据（满足其一即可）：
   - **复用依据**：对外公开的业务能力会被 2 个及以上、来自不同业务领域的 BC 复用
   - **变化依据**：内部一部分能力与另一部分能力的变化原因不同（违背单一职责原则）
   - **技术栈依据**：需要采用完全不同的技术栈才能实现（如实时流处理与关系型数据查询）
   - **质量属性依据**：在安全、性能、资源占用方面有独立且与其他功能显著差异的质量属性要求

## 防腐层隔离

1. 上游为外部遗留系统时，必须通过防腐层（ACL）隔离。
2. ACL **只做模型翻译**，**禁止在 ACL 中编写业务规则**。

## 映射原则

- **核心子领域** → 核心能力层
- **支撑子领域** → 公共能力层
- **通用子领域** → 公共能力层`;
