/**
 * Sparrow Explore skill template.
 *
 * This skill identifies business services from raw requirements and
 * produces a structured business service definition document.
 */

import { registerSkillTemplate } from '../core/skill-generation.js';

const EXPLORE_BODY = `# Sparrow Explore — 需求探索与业务服务识别

## 执行顺序检查

在执行之前，请检查当前阶段是否合适：

- **本阶段**：sparrow-explore（第 1 步，共 6 步）
- **所属层级**：产品级（product-level）
- **前置条件**：无（这是整个 DDD 流程的第一步）
- **输入要求**：用户需要提供原始需求文档或需求描述
- **检查逻辑**：
  - 如果当前目录下不存在 \`docs/sparrow/requirement/prd-business.md\`，可以继续执行
  - 如果已经存在，请参考下方"输出文件存在性检查"章节处理

---

## 🛑 输出文件存在性检查（必须在生成前执行）

在开始生成内容之前，请检查以下输出文件是否已经存在：

- \`docs/sparrow/requirement/prd-business.md\`
- \`docs/sparrow/requirement/prd-quanlity.md\`

如果任一文件已存在，请**让用户进行一次选择**（该选择将应用于所有已存在的文件）：

- **跳过 (skip)**：保留已有文件，不执行任何生成操作，停止执行
- **覆盖 (overwrite)**：删除已有文件，重新生成全新的内容
- **更新 (update)**：在已有文件基础上进行修改和完善

> ⚠️ 一次命令只确认一次，用户的选择应用于所有输出文件。

---

## 📋 project.md 更新

完成输出后，**必须**更新 \`docs/sparrow/project.md\`：

1. 如果 \`project.md\` 不存在，根据当前项目信息创建它
2. 在"文档索引"部分，更新 \`requirement/prd-business.md\` 和 \`requirement/prd-quanlity.md\` 的链接
3. 将对应条目的状态从 \`_待生成 (sparrow-explore)_\` 更新为 \`_v{version}_\`（使用实际版本号）
4. 更新文件头部的"最后更新"时间戳

**project.md 路径**: \`docs/sparrow/project.md\`

---

## 📌 版本元数据管理

所有输出的文档文件**必须在文件开头**包含版本元数据块：

\`\`\`markdown
<!--
  version: v1.0
  last-updated: {ISO_8601_TIMESTAMP}
  generated-by: sparrow-explore
  sparrow-version: {从 .sparrow/sparrow.json 读取}
-->
\`\`\`

**版本规则**:
- **新文档**: 使用 \`v1.0\`
- **更新已有文档**: 读取现有版本号，递增次版本号（\`v1.0\` → \`v1.1\`）
- **重大重写**: 递增主版本号（\`v1.x\` → \`v2.0\`）
- 每次修改都**必须变更**版本号

**操作步骤**:
1. 检查目标文件是否已存在
2. 如果存在，读取文件开头 \`<!--\` 注释块中的 \`version:\` 字段并递增
3. 在文件开头添加或更新版本元数据块
4. 继续生成文档正文

---

## 整体流程

sparrow-explore 分为两个阶段：

1. **阶段一：互动式需求探索（Grill Me）** — 通过一问一答的方式，深度挖掘用户需求
2. **阶段二：需求文档生成** — 将探索结果整理为功能需求文档（prd-business.md）和系统质量属性文档（prd-quanlity.md）

---

## 阶段一：互动式需求探索（Grill Me）

### 角色定义

在此阶段，你是一名**需求分析师**，通过逐步提问的方式，深入挖掘用户需求的每一个细节，确保需求被全面覆盖且无歧义。

### 核心流程

1. **启动探索**：基于用户提供的原始需求，识别需求覆盖的核心范围
2. **一问一答**：每次只提出一个具体问题，并提供推荐的答案
3. **用户响应**：用户可以对每个问题回答"是"（接受推荐答案）、"否"（否定推荐答案）或"改"（修改推荐答案）
4. **持续深入**：沿着需求的每个分支逐步深入，直到覆盖所有关键维度
5. **结束探索**：当问题的维度完整覆盖需求范围后，输出**快速总结**

### 提问维度

在探索过程中，至少覆盖以下维度的问题：

| 维度 | 说明 | 示例问题 |
|------|------|---------|
| **参与者识别** | 哪些角色会使用系统？ | "除了买家之外，是否有商家和平台管理员这两个参与者？" |
| **核心流程** | 每个参与者的主要业务操作 | "买家的核心操作是否包括浏览商品、下单、支付和查看订单状态？" |
| **业务规则** | 重要的业务约束和规则 | "订单提交后，如果30分钟内未支付，是否应自动取消？" |
| **边界条件** | 系统的输入输出边界 | "系统是否需要对接第三方物流系统来获取物流单号？" |
| **异常场景** | 出错或异常时的处理方式 | "支付超时或支付失败时，系统如何处理？" |
| **质量属性** | 性能、安全、可用性等非功能需求 | "在用户登录和支付环节，是否要求更高的安全性？" |

### 提问规则

1. **每次只问一个问题**：不要一次提出多个问题，确保用户能逐个思考和确认
2. **给出推荐答案**：基于你对需求的理解，每次都要给出推荐的答案供用户确认
3. **明确确认提示**：提出每个问题后，必须在问题末尾明确提示用户确认选项：
   \`\`\`
   请确认：是（接受推荐答案） / 否（否定推荐答案） / 修改（提供修改后的答案）
   \`\`\`
4. **按维度有序推进**：优先覆盖核心流程和参与者，再深入业务规则和边界条件，最后覆盖异常场景和质量属性
5. **闭环确认**：当某一维度的所有问题都得到用户确认后，总结该维度的结论，再进入下一维度
6. **不过度提问**：如果用户的问题已经清晰覆盖某一维度，不要为了提问而提问

### 快速总结

当所有维度都探索完毕后，输出快速总结，包括：
- 识别的全部参与者及其职责
- 核心业务流程概述
- 关键业务规则
- 主要的质量属性要求

---

## 阶段二：需求文档生成

在完成阶段一的互动式需求探索后，基于确认的需求，生成两份文档：

### 输出文档

| 文档 | 路径 | 内容 |
|------|------|------|
| 功能需求文档 | \`docs/sparrow/requirement/prd-business.md\` | 功能性需求，按照业务服务规范编写 |
| 系统质量属性文档 | \`docs/sparrow/requirement/prd-quanlity.md\` | 非功能性需求，从性能、安全、高可用等角度描述 |

---

## 业务服务识别（生成 prd-business.md）

### 角色定义

在此阶段，你是一名专业的**业务架构师**，负责从需求探索结果中识别业务服务。业务服务是业务架构的核心组成部分，是连接业务需求与技术实现的重要桥梁。

---

## 变更模式（revise）— 活动变更下的需求 delta

> **⚠️ 门控声明（向后兼容硬性约束）**：本节仅在**检测到活动变更**时进入。**若当前为首次需求、无活动变更，请忽略本节，完全按上文原始流程（输出文件存在性检查 skip/overwrite/update）执行，行为须与未引入本节前完全一致。**

### 触发条件

\`\`\`
活动变更存在，当且仅当满足以下任一：
  - docs/sparrow/changes/ 目录存在，且其中包含「未归档」的变更文件夹
  - docs/sparrow/project.md 的「变更管理」块中「当前活动 change-id」非空
\`\`\`

- **不满足** → 普通模式，跳过本节，按原流程。
- **满足** → 进入 revise 模式，加载 \`docs/sparrow/changes/{change-id}/proposal.md\`，对需求做**增量 delta** 而非全量重生成。

> 完整约定（目录结构、delta 格式、BC 档位判定、决策框架）见 \`docs/prd/sparrow-change-management.md\` 与 **sparrow-arch 的「变更处理 / revise」章节**（权威定义）。

### revise 模式行为

1. 读取现有 \`docs/sparrow/requirement/prd-business.md\`（基线业务服务全集）。
2. 将用户提供的「新原始需求」与现有业务服务逐项比对，识别：
   - **ADDED**：新需求引入的、现有未覆盖的业务服务
   - **MODIFIED**：现有业务服务因需求变化需调整（描述/流程/验收）
   - **REMOVED**：现有业务服务因需求缺口或消失而不再需要
3. 将 delta 写入 \`docs/sparrow/changes/{change-id}/deltas/requirement/prd-business.md\`，用 \`## ADDED Requirements\` / \`## MODIFIED Requirements\` / \`## REMOVED Requirements\` 分区（OpenSpec 格式）。
4. **原地更新** \`docs/sparrow/requirement/prd-business.md\`：合并 ADDED、应用 MODIFIED、标注 REMOVED 为废弃，保持单一天然事实来源。
5. 同样的 delta 流程应用于 \`docs/sparrow/requirement/prd-quanlity.md\`。
6. 更新版本元数据：版本号递增（v1.0→v1.1），并在元数据块追加 \`change-id: {change-id}\`（基线不追加此字段）。
7. 更新 \`docs/sparrow/project.md\`：同步 \`requirement/prd-business.md\` 和 \`requirement/prd-quanlity.md\` 的版本状态。

> explore 在 revise 模式下**始终执行**（任何变更都从需求 delta 开始）；下游阶段按 BC 档位按需触发（见 sparrow-arch 变更处理章节）。

## 核心要求：全面覆盖原始需求

- **必须全面覆盖用户提供的原始需求**：仔细分析原始需求文档中的每一个功能点、每一个业务场景、每一个用户需求，确保都被识别为相应的业务服务
- **不能遗漏任何业务功能**：对于原始需求中提到的所有业务功能，都必须有对应的业务服务，不能有任何遗漏
- **完整性是评估的关键指标**：业务服务定义文档的完整性将基于是否全面覆盖原始需求进行评估

## 第一性原理

### 1. 价值导向原则
- 每个业务服务必须为业务参与者创造明确的价值
- 业务服务应该解决具体的业务问题或满足特定的业务需求
- 避免识别那些没有实际业务价值的纯技术功能

### 2. 完整性原则
- 业务服务必须是一次完整的功能交互，从触发到完成
- 业务服务应该包含成功和失败的所有可能路径
- 业务服务应该考虑所有相关的业务规则和约束

### 3. 独立性原则
- 业务服务应该相对独立，减少与其他服务的耦合
- 每个业务服务应该有明确的边界和职责
- 业务服务之间应该通过明确的接口进行交互

## 业务服务定义

**业务服务**是业务的参与者（actor）主动向目标系统（后端）发起服务请求，完成一次连续而完整的功能交互，并体现服务价值的业务行为。

### 关键理解：业务服务 ≠ 用例

**业务服务不是用例（Use Case）。** 用例可能聚合了多个操作，而业务服务是对目标系统发起的**一次请求**。

将目标系统视为一个**黑盒子**：
- 参与者向黑盒子**发起一次请求** → 黑盒子内部处理 → 黑盒子返回结果
- 这一次请求就是一个业务服务
- 不同的请求就是不同的业务服务

### 粒度原则：一次请求 = 一个业务服务

**每个业务服务对应参与者向目标系统发起的一次独立请求。** 应该按照"操作"而非"模块"来划分服务。

**正确示例 — 管理个人资料**（应为 4 个独立的业务服务）：
1. **查询个人资料**：用户向系统请求查看自己的资料信息
2. **更新个人资料**：用户向系统请求修改自己的资料信息
3. **删除个人资料**：用户向系统请求删除自己的账户
4. **新增个人资料**：用户向系统请求创建新的个人资料

**错误示例 — 请勿模仿**：
- ❌ "管理个人资料"作为 1 个业务服务（它包含了增删改查多个请求，是模块而非服务）
- ❌ "用户管理"作为 1 个业务服务（它是一个功能模块，包含多个独立的请求）

### 判断标准

在识别业务服务时，通过以下问题自检（每个问题独立判断即可）：

- **请求次数**：参与者向目标系统发起了几次请求？如果一次请求就能独立完成有意义的业务功能，它就是一个业务服务
- **独立价值**：该请求完成后，是否产生了明确的、对参与者有价值的业务结果？
- **独立触发**：该请求是否可以独立触发，而不需要依赖其他请求先执行？

如果以上三个问题的答案都是"是"，则该请求应被识别为一个独立的业务服务。

### 参与者类型

1. **用户（user）**：关心业务服务的业务角色或业务部门，通过执行某个操作触发服务请求
2. **策略（policy）**：封装了业务规则的定时器，一旦满足该业务规则，就能够主动触发向目标系统发起服务请求
3. **伴生系统（accompanying system）**：位于目标系统之外，只要是由伴生系统发起向目标系统的服务请求，就是业务服务的参与者

## 业务服务组成元素

每个业务服务必须包含以下组成元素：

### 1. 服务编号
- 格式：\`{系统名称}-{序号}\`
- 示例：\`EC-001\`、\`TRIBE-001\`

### 2. 服务名
- 格式：动词短语形式
- 示例：\`提交订单\`、\`用户注册\`、\`申请贷款\`

### 3. 服务描述
- 格式：作为<角色>，我想要<服务功能>，以便<服务价值>

### 4. 触发事件
- 描述：触发该业务服务的事件

### 5. 基本流程
- 描述：用于表现业务服务的主流程，即执行成功的业务场景
- 格式：编号列表，每个步骤清晰明确

### 6. 替代流程
- 描述：用于表现业务服务的扩展流程，即执行失败的业务场景
- 格式：编号+字母（如1a、2a），对应基本流程的步骤

### 7. 验收标准
- 描述：一系列可以接受的条件或者业务规则
- 格式：要点形式列举，可测试、可验证

## 识别约束条件

### 1. 执行步骤约束
- 业务服务的执行步骤不包括线下执行的步骤
- 业务服务的执行步骤仅限于系统后端收到请求执行的一次连续而完整的功能交互
- 不包括前端的任何操作

### 2. 服务名称约束
- 业务服务的名称应采用动词短语，以动宾结构构成
- 避免使用技术术语

### 3. 边界约束
- 业务服务应该有明确的输入和输出
- 每个业务服务是参与者向目标系统发起的**一次独立请求**，不应包含多个独立操作
- 避免识别过于庞大（将多个操作聚合为一个服务）或过于细粒度（将一个操作拆分为多次请求）的服务
- 判断标准：如果两个操作可以独立触发、独立完成且各有独立的业务价值，则它们应该被识别为两个业务服务

## 业务服务示例

\`\`\`
服务编号：I-001
服务名：提交订单
服务描述：
作为买家，
我想要提交订单，
以便买到我心仪的商品。

触发事件：买家点击"提交订单"按钮

基本流程：
1. 验证订单有效性；
2. 验证订单所购商品是否缺货；
3. 插入订单；
4. 从购物车中移除所购商品；
5. 锁定库存；
6. 通知买家订单已提交。

替代流程：
1a. 如果订单无效，给出提示信息；
2a. 如果所购商品缺货，给出提示信息；
3a. 若订单提交失败，给出失败原因。

验收标准：
1. 订单需要包含客户ID、配送地址、联系信息及已购商品的订单项；
2. 订单项中商品的购买数量要小于或等于库存量；
3. 订单提交成功后，订单状态更改为"已提交"；
4. 购物车对应商品被移除；
5. 在完成支付或取消订单前，需要锁定对应数量的商品；
6. 买家收到订单已提交的通知。
\`\`\`

## 识别步骤指导

### 步骤1：需求分析
1. 仔细阅读需求规格说明书或调研需求，全面理解所有业务需求
2. 识别所有的业务参与者（用户、策略、伴生系统）
3. 理解每个参与者的业务目标和价值诉求
4. 全面梳理原始需求中的所有功能点，确保不遗漏任何业务功能

### 步骤2：功能识别
1. 全面分析原始需求文档，识别所有需要系统支持的业务功能点
2. 确保全面覆盖原始需求：仔细检查原始需求文档中的每一个功能点
3. 不遗漏任何业务功能
4. 将相关功能组织成业务服务
5. 确保每个业务服务都有明确的触发事件

### 步骤3：流程设计
1. 为每个业务服务设计基本流程
2. 识别可能的异常情况，设计替代流程
3. 确保流程的完整性和一致性

### 步骤4：验收标准制定
1. 为每个业务服务制定明确的验收标准
2. 确保验收标准可测试、可验证
3. 覆盖功能性和非功能性要求

### 步骤5：文档化
1. 按照标准格式编写业务服务文档
2. 确保所有组成元素都完整
3. 检查是否符合约束条件
4. 验证是否全面覆盖了原始需求中的所有业务功能

## 输出要求

识别完成后，按以下要求生成两份文档。

### 文档一：功能需求文档

写入 **\`docs/sparrow/requirement/prd-business.md\`**：

\`\`\`markdown
# {系统名称}功能需求文档

## 1 {业务服务1名称}
**服务编号：** {编号}
**服务名：** {服务名}
**服务描述：**
作为{角色}，
我想要{服务功能}，
以便{服务价值}。

**触发事件：** {触发事件}

**基本流程：**
1. {步骤1}；
2. {步骤2}；
...

**替代流程：**
1a. {异常情况1}；
2a. {异常情况2}；
...

**验收标准：**
1. {标准1}；
2. {标准2}；
...

---

## 2 {业务服务2名称}
...
\`\`\`

### 文档二：系统质量属性文档

写入 **\`docs/sparrow/requirement/prd-quanlity.md\`**。

根据需求探索中涉及的质量属性要求，从以下角度（**仅覆盖需求中明确涉及的维度**，未涉及的维度不提供）：

#### 质量属性维度

| 维度 | 需要关注的情况 | 如何描述 |
|------|--------------|---------|
| **性能** | 系统对响应时间、吞吐量有明确要求 | 说明关键场景的性能指标（如接口响应时间、并发处理能力） |
| **安全** | 涉及用户认证、数据加密、权限控制、敏感操作审计 | 说明安全策略和防护要求（如认证方式、加密标准、防攻击措施） |
| **高可用** | 系统需要 7×24 运行，不能有单点故障 | 说明可用性要求（如可用性指标、容灾策略、故障恢复时间） |
| **高并发** | 系统面临大量用户同时访问 | 说明并发场景和处理策略（如峰值 TPS、限流策略、弹性伸缩） |
| **健壮性** | 系统需要处理异常输入、外部系统故障等 | 说明容错和降级策略（如异常处理机制、服务降级、熔断策略） |
| **可扩展性** | 系统未来可能需要扩展功能或对接新系统 | 说明扩展要求（如插件化设计、对外开放 API、多租户支持） |
| **数据一致性** | 涉及跨服务事务或数据同步 | 说明一致性要求（如强一致性/最终一致性、事务边界、补偿机制） |
| **合规性** | 涉及法律法规、行业标准 | 说明合规要求（如数据隐私条例、审计日志规范、行业认证标准） |

**文档格式**：

\`\`\`markdown
# {系统名称}系统质量属性文档

## 1 性能
[如果需求中明确了性能要求，在此描述]
[如果需求中未涉及性能要求，省略此章节]

## 2 安全
[如果需求中明确了安全要求，在此描述]
[如果需求中未涉及安全要求，省略此章节]

## 3 高可用
...

## 4 高并发
...

## 5 健壮性
...

## 6 可扩展性
...

## 7 数据一致性
...

## 8 合规性
...
\`\`\`

> ⚠️ **重要**：只输出需求探索中实际涉及的维度。没有涉及的维度**不要**提供空章节或"暂不考虑"等占位文字，直接省略。

## 质量检查清单

在完成需求探索和文档生成后，请检查以下项目：

### 阶段一：需求探索
- [ ] 所有需求探索维度（参与者、核心流程、业务规则、边界条件、异常场景、质量属性）都已覆盖
- [ ] 每个问题都给出了推荐答案
- [ ] 用户对所有关键问题都做出了确认（是/否/改）
- [ ] 探索完毕后输出了快速总结
- [ ] 没有为了提问而提问的冗余问题

### 阶段二：功能需求文档（prd-business.md）
- [ ] **业务服务完整性**：所有原始需求中的业务功能都已被识别为业务服务，没有遗漏
- [ ] **需求覆盖完整性**：原始需求文档中的每个功能点、每个业务场景、每个用户需求都有对应的业务服务
- [ ] 每个业务服务都有明确的参与者
- [ ] 每个业务服务都有明确的价值主张
- [ ] 每个业务服务都有明确的触发事件
- [ ] 基本流程覆盖了主要的成功路径
- [ ] 替代流程覆盖了主要的异常情况
- [ ] 验收标准可测试、可验证
- [ ] 服务名称采用动词短语形式
- [ ] 执行步骤不包括线下操作
- [ ] 执行步骤仅限于系统后端功能
- [ ] 业务服务之间有清晰的边界
- [ ] **一次请求原则**：每个业务服务只对应参与者向目标系统发起的一次独立请求
- [ ] **非用例检查**：没有将"模块"或"用例"误识别为业务服务（如将增删改查合并为一个服务）
- [ ] 对于同一领域对象的不同操作（查询、新增、修改、删除），已识别为独立的业务服务

### 阶段二：质量属性文档（prd-quanlity.md）
- [ ] 仅覆盖了需求探索中实际涉及的质量属性维度
- [ ] 未涉及的维度已省略（无空章节或占位文字）
- [ ] 每个质量属性都有具体的指标或要求描述
- [ ] 安全相关要求（如认证、加密、权限）已明确
- [ ] 性能关键场景的量化指标已明确
- [ ] 高可用和容灾要求（如适用）已明确

## 完成后的下一步

✅ 完成 sparrow-explore 后，请执行 **sparrow-arch**（产品级）—— 基于 prd-business.md 和 prd-quanlity.md，划分子领域并映射限界上下文，定义系统的业务架构和应用架构。`;

export function register(): void {
  registerSkillTemplate('sparrow-explore', () => EXPLORE_BODY);
}
